<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %} | Flaskr</title>
</head>

<body>
    <header>
        <h1>Flaskr | {{ self.title() }}</h1>
    </header>
    <main>
        {% if error %}
        <div>{{ error }}</div>
        {% endif %}
        {% block content %}{% endblock %}
    </main>
</body>

</html>
